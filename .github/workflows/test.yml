name: 'Test'
on:
  pull_request:
    types: [ opened, reopened, synchronize ]

jobs:
  test:
    uses: UKHomeOffice/sas-github-workflows/.github/workflows/test-npm.yml@v2
    with:
      installCommand: 'ci --include=dev --omit=optional'
      buildCommand: 'build-prod'
      nodeVersionMatrix: '[ "24.x" ]'
